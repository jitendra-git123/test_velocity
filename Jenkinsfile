node() {
  
  def check
  stage('prepare') {
      check=checkout scm
      echo "${check}"
        echo ">>>>>>>${check.GIT_COMMIT}"
       }

  stage('build') {
    echo ">>>>>>>123"
        }
  
  stage('Dev Deployment') {
  	echo ">>>>>>>12345"
  } 
}
